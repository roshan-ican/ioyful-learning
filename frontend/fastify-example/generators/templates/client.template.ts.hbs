
import { crudFactory } from '@/data/client/curd-factory';

import { API_ENDPOINTS } from '@/data/client/api-endpoints';
import { QueryOptions, {{pascalCase name}}, {{pascalCase name}}Input } from '@/types';

export const {{pascalCase name}}Client = {
  ...crudFactory<{{pascalCase name}}, QueryOptions, {{pascalCase name}}Input>(API_ENDPOINTS.{{constantCase name}}),
};
