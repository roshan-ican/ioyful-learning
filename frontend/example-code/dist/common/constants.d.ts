export declare const CSV_FILE_LIMITS: {
    fieldNameSize: number;
    fieldSize: number;
    fields: number;
    fileSize: number;
    files: number;
    headerPairs: number;
    parts: number;
};
export declare enum PROCESS_STATUS {
    INITIATED = "INITIATED",
    SUCCESSFUL = "SUCCESSFUL",
    FAILED = "FAILED"
}
